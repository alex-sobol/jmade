bin/cqlsh
CREATE KEYSPACE IF NOT EXISTS sample WITH replication = {'class':'SimpleStrategy', 'replication_factor':1};

In properties:
spring.data.cassandra.keyspace-name=sample

CREATE TABLE IF NOT EXISTS  sample.message_log (id UUID,actorId varchar,type varchar,createdDate timestamp,content varchar,primary key((id)));